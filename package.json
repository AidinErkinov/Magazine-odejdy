{
  "name": "magazine-odejdy",
  "version": "1.0.0",
  "description": "Magazin",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --update --style=expanded assets/scss:assets/css",
    "sass-prod": "sass --no-source-map --style=compressed assets/scss:assets/css",
    "build": "babel main.js -d dist --presets es2015",
    "watch": "babel main.js -d dist --presets es2015 -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "sass": "^1.69.5"
  }
}
