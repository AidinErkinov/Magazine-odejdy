import {Program} from './program.js'; 
var program = new Program (); 
program.execute();
program.execute();
program.execute();
program.execute();
program.execute();





























/* function func1 () {
    surname = "Apple";
    let surname, name;
    if (true) {
        console.log(name);
    } 
    console.log(name);
}
/* hoisting важно для интеревью. Вобщем можно дать значение, а потом через let и var дать переменную внутри блока имеет значение этого блока

function func2 () {
    var name;
    surname = "Dewa";
    let surname;
    if (true) {
        console.log(name);
    } 
    console.log(name);
}


func1 ()
func2 ()
 */ 